# Namespaces and public APIs

Browse the generated namespace tree to find the public contracts for modeling, querying, execution, tracking, mutations, migrations, and provider configuration.

Application development normally begins with the `Nodal.Core` namespace. The execution and provider contracts are useful when building a new database provider or integrating a custom transport.
