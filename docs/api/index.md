# Nodal Framework API reference

This reference is generated from the public XML documentation in `Nodal.Core`, `Nodal.Migrations`, `Nodal.Neo4j`, and `Nodal.TigerGraph`.

Start with `NodalContext`, `GraphSet<TNode>`, and `RelationSet<TSource, TRelation, TTarget>` for application code. Provider and execution contracts are intended for provider authors and advanced integrations.

Use the [hand-written guide](/docs/intro) for concepts and complete workflows.
