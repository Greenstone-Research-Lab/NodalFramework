name: Deploy Documentation

on:
  push:
    branches: [master]
    paths:
      - 'website/**'
      - 'docs/api/**'
      - 'src/**'
      - 'eng/build-docs.ps1'
      - '.config/dotnet-tools.json'
      - '.github/workflows/deploy-documentation.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nodal-docs-production
  cancel-in-progress: false

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  deploy:
    name: Publish Cloudflare Pages site
    if: github.event_name == 'workflow_dispatch' || vars.NODAL_DOCS_DEPLOY_ENABLED == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: documentation-production

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Install .NET SDK
        uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: website/package-lock.json

      - name: Build static site
        shell: pwsh
        run: ./eng/build-docs.ps1

      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy website/build --project-name nodalframework --branch master
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
